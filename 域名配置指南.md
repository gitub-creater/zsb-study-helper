# 域名配置指南 - 解决国内访问问题

## 问题说明

Vercel 默认域名 *.vercel.app 在中国大陆无法访问，需要绑定自定义域名。

---

## 方案对比

| 方案 | 成本 | 难度 | 国内访问 | 备注 |
|------|------|------|----------|------|
| 国内备案域名 | 域名费 50-100元/年 | 简单 | 优秀 | 推荐 |
| Cloudflare CDN | 免费 | 中等 | 良好 | 需要域名 |
| 国外域名 | 域名费 10-50元/年 | 简单 | 可能被墙 | 不推荐 |
| 自建代理 | 服务器费 | 复杂 | 优秀 | 高级用户 |

---

## 方案 1: 使用国内备案域名 (推荐)

### 前提条件
- 拥有已备案的域名 (如 example.com)
- 域名在阿里云/腾讯云/其他服务商管理

### 步骤 1: 在 Vercel 添加域名

1. 访问 https://vercel.com/dashboard
2. 选择项目 zsb-study-helper
3. 点击 Settings → Domains
4. 输入子域名: zsb.yourdomain.com
5. 点击 Add

Vercel 会显示需要配置的 DNS 记录。

### 步骤 2: 配置 DNS 记录

#### 阿里云配置示例
1. 登录 阿里云控制台
2. 进入 域名 → 解析设置
3. 点击 添加记录
4. 填写：
   - 记录类型: CNAME
   - 主机记录: zsb
   - 记录值: cname.vercel-dns.com
   - TTL: 600 (10分钟)
5. 点击 确定

#### 腾讯云配置示例
1. 登录 腾讯云控制台
2. 进入 DNS 解析 DNSPod
3. 选择域名 → 添加记录
4. 填写：
   - 主机记录: zsb
   - 记录类型: CNAME
   - 记录值: cname.vercel-dns.com
   - TTL: 600
5. 保存

#### Cloudflare 配置示例
1. 登录 Cloudflare Dashboard
2. 选择域名
3. DNS → Add record
4. 填写：
   - Type: CNAME
   - Name: zsb
   - Target: cname.vercel-dns.com
   - Proxy status: DNS only (灰色云)
5. Save

### 步骤 3: 等待生效

- DNS 生效时间: 10 分钟 - 24 小时
- Vercel 会自动配置 SSL 证书 (Let's Encrypt)
- 证书配置时间: 1-5 分钟

### 步骤 4: 验证

在浏览器访问: https://zsb.yourdomain.com

如果显示应用首页，说明配置成功！

---

## 方案 2: 使用 Cloudflare CDN

### 优势
- 免费 CDN 加速
- 自动 HTTPS
- 防御 DDoS
- 可能绕过部分网络限制

### 步骤 1: 注册 Cloudflare

1. 访问 https://dash.cloudflare.com/sign-up
2. 注册账号

### 步骤 2: 添加域名

1. Dashboard → Add a Site
2. 输入你的域名: yourdomain.com
3. 选择 Free 计划
4. 点击 Continue

### 步骤 3: 修改域名 DNS

Cloudflare 会显示 2 个 Nameserver 地址，例如：
- ns1.cloudflare.com
- ns2.cloudflare.com

到你的域名注册商修改 DNS 服务器为这 2 个地址。

### 步骤 4: 添加 DNS 记录

在 Cloudflare Dashboard:
1. DNS → Add record
2. 填写：
   - Type: CNAME
   - Name: zsb
   - Target: cname.vercel-dns.com
   - Proxy status: Proxied (橙色云)
3. Save

### 步骤 5: 配置 SSL

1. SSL/TLS → Overview
2. 选择 Full 或 Full (strict)

### 步骤 6: 在 Vercel 添加域名

1. Vercel → Settings → Domains
2. 添加: zsb.yourdomain.com
3. Vercel 会自动验证

---

## 方案 3: 自建反向代理 (高级)

### 前提条件
- 拥有国内云服务器 (阿里云/腾讯云)
- 服务器已备案
- 熟悉 Nginx 配置

### Nginx 配置示例

创建配置文件: /etc/nginx/sites-available/zsb

server {
    listen 80;
    server_name zsb.yourdomain.com;
    
    # 自动跳转 HTTPS
    return 301 https://\System.Management.Automation.Internal.Host.InternalHost\;
}

server {
    listen 443 ssl http2;
    server_name zsb.yourdomain.com;
    
    # SSL 证书配置
    ssl_certificate /path/to/fullchain.pem;
    ssl_certificate_key /path/to/privkey.pem;
    
    # 反向代理到 Vercel
    location / {
        proxy_pass https://zsb-study-helper.vercel.app;
        proxy_set_header Host zsb-study-helper.vercel.app;
        proxy_set_header X-Real-IP \;
        proxy_set_header X-Forwarded-For \;
        proxy_set_header X-Forwarded-Proto \;
        
        # 缓存配置
        proxy_cache_bypass \;
        proxy_redirect off;
    }
}

启用配置:
sudo ln -s /etc/nginx/sites-available/zsb /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

---

## 更新前端配置

### 方法 1: 环境变量 (推荐)

在 Vercel 配置:
1. Settings → Environment Variables
2. 添加:
   - VITE_CLOUD_API_URL = https://zsb.yourdomain.com

### 方法 2: 修改代码

编辑 .env.production:
VITE_CLOUD_API_URL=https://zsb.yourdomain.com

重新部署前端。

---

## 验证域名配置

### 1. DNS 检查

nslookup zsb.yourdomain.com

应该返回 Vercel 的 IP 地址或 CNAME 记录。

### 2. SSL 检查

https://www.ssllabs.com/ssltest/analyze.html?d=zsb.yourdomain.com

应该显示 A 或 A+ 等级。

### 3. 访问测试

curl -I https://zsb.yourdomain.com

应该返回 200 OK 和正确的响应头。

### 4. 功能测试

在浏览器访问: https://zsb.yourdomain.com

- 页面正常加载
- 可以发送验证码
- 可以登录

---

## 常见问题

### Q1: DNS 配置后仍无法访问？

- 等待 DNS 生效 (最多 24 小时)
- 清除浏览器缓存和 DNS 缓存
- 使用 dig 或 nslookup 检查 DNS

### Q2: 显示 SSL 证书错误？

- Vercel 自动配置证书需要 1-5 分钟
- 确认域名 DNS 已正确指向
- 在 Vercel Dashboard 手动触发证书更新

### Q3: 显示 404 Not Found？

- 确认在 Vercel 已添加该域名
- 检查 DNS CNAME 记录是否正确
- 等待 Vercel 配置完成

### Q4: Cloudflare 显示 522 错误？

- 在 Cloudflare 将 SSL 模式改为 Full
- 确认 Vercel 域名配置正确
- 暂时关闭 Proxy (灰色云) 测试

### Q5: 国内仍然无法访问？

- 尝试不同网络环境 (移动/联通/电信)
- 使用 Cloudflare CDN
- 考虑自建反向代理

---

## 成本估算

### 方案 1: 国内备案域名
- 域名注册: 50-100 元/年
- 备案: 免费 (需要服务器)
- SSL 证书: 免费 (Let's Encrypt)
- 总计: 50-100 元/年

### 方案 2: Cloudflare CDN
- Cloudflare: 免费
- 域名: 10-100 元/年
- 总计: 10-100 元/年

### 方案 3: 自建代理
- 服务器: 100-500 元/年
- 域名: 50-100 元/年
- 带宽: 按实际使用
- 总计: 150-600 元/年

---

## 推荐域名注册商

### 国内
- 阿里云 (推荐): https://wanwang.aliyun.com
- 腾讯云: https://dnspod.cloud.tencent.com
- 华为云: https://www.huaweicloud.com/product/domain.html

### 国外
- Cloudflare: https://www.cloudflare.com/products/registrar/
- Namecheap: https://www.namecheap.com
- Google Domains: https://domains.google

---

版本: v1.0
更新时间: 2026-09-19
返回: 快速开始.md
